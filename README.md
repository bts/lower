# lower

Lowering a surface syntax into different intermediate representations (just ANF for now).

## Requirements

- nix
- `nix flake` enabled
- direnv

## Getting started

- `direnv allow`
- `hpack && ghcid`